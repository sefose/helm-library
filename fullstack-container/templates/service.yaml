apiVersion: v1
kind: Service
metadata:
  name: {{ include "fullstack-container.fullname" . }}
  labels:
    {{- include "fullstack-container.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 1080
      protocol: TCP
      name: http
  selector:
    {{- include "fullstack-container.selectorLabels" . | nindent 4 }}
