apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "fullstack-container.fullname" . }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi