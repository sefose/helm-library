apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fullstack-container.fullname" . }}-test-connection"
  labels:
    {{- include "fullstack-container.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fullstack-container.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
